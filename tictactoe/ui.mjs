export class TicTacToe_UI {

	static MAIN_MENU_OPTION = {
		START: 'Start game',
		EXIT: 'Exit'
	}
	
	constructor() {
		this.mainMenuOptionSelected = // condition entre 0 a 1
		this.caseSelected = // condition entre 0 a 8
	}
	
	// Affiche le Menu du jeu
	showMainScreen(ttt) { }
	// Affiche le curseur
	showMenuCursor() { }
	// Affiche le jeu (contenant un TicTacToe)
	showGame() { }
	// Affiche le curseur dans le jeu
	showGameCursor() { }
	// Affiche l'ecran de fin du jeu
	showEndGame() { }
	// Prend la case choisie
	setCaseSelected(casee) { }
	// Prend l'option du menu choisie
	setMainMenuOptionSelected(menu) { }


}	